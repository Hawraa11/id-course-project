fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
python-dotenv==1.0.1

# Note: these are pinned to versions current as of this skeleton's creation.
# After running `pip install -r requirements.txt`, verify actual installed
# versions with `pip freeze` and update this file to match if they differ.
